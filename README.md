
  # nexo

  This is a code bundle for nexo. The original project is available at https://www.figma.com/design/Cx1A19PT5US6N8pzUZ3LYo/nexo.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  